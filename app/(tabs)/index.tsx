import IsometricMap from "@/components/IsometricMap";

/**
 * Home Screen — Isometric Tile Farm Map
 *
 * Full-screen isometric tile map inspired by Township's farming game style.
 * Supports pan, zoom, and tile placement.
 */
export default function HomeScreen() {
  return <IsometricMap />;
}
